# ARIAC PDDL Image Grounding 进度总结

最后更新：2026-06-02

项目目录：`/home/pc/pddl_image`

## 1. 当前任务定义

目标是从 ARIAC 场景图像预测 PDDL `(:init ...)` 初始状态。

当前只使用真实 `(:init ...)` atoms 作为监督，不使用 goal，也不使用 VL conversation 文本。输出必须是合法 symbolic init state，主要 predicates 包括：

```text
part_at(part, location)
on(part, part)
clear(part)
handempty
robot_at(table)
```

当前核心建模方式不是直接独立预测所有 atoms，而是预测 placement factor：

```text
place(part) in {other active part, table, placement region}
```

然后由 deterministic decoder 推导：

```text
place(part) = location -> part_at(part, location)
place(part) = other_part -> on(part, other_part)
no part directly on p -> clear(p)
```

这样可以把 atom-level multi-label prediction 转成更符合 PDDL 结构的 support/location assignment 问题。

## 2. 当前数据状态

数据目录：

```text
data/ariac
```

清理 duplicate active parts 后：

```text
usable samples = 152
excluded duplicate-part samples = 34
parts = 9
locations = 5
canonical init atoms = 132
```

最近统一使用：

```text
--exclude-duplicate-parts
--split-seed 42
--init-seed 42
--test-size 100
```

因此 train pool 为：

```text
152 - 100 = 52
```

注意：`K=52` 和 `K=50` 是不同训练规模，结果不能混为同一个 baseline。

## 3. Frozen DINOv3 提供什么

当前主线使用 frozen DINOv3 H+ 640 dense features：

```text
image
-> DINOv3 H+ /16 at 640x640
-> 40 x 40 = 1600 dense patch tokens
-> 1280-d feature per token
-> append spatial coords
-> 1290-d token
```

主特征缓存保留在：

```text
experiments/ariac_52_100_hplus640_d256_coords_structcf_20260602/ariac_dinov3_raw_s640_l1_last_coords_features.pt
```

DINOv3 本身只提供匿名 dense visual tokens。它不直接知道：

```text
blue_battery
green_pump
regulator_placement
part_at
on
clear
```

所以后续小模块的任务不是重新学习视觉世界，而是把 frozen dense visual prior 适配到 PDDL symbol / candidate / legal state 空间。

## 4. 小模块的正确定位

当前结论非常明确：

```text
小模块 = frozen DINOv3 到 PDDL 结构空间之间的低参数、强归纳偏置 adapter
```

它不应该被定位成：

```text
小型 detector
小型 mask learner
小型 geometry learner
复杂 contact NN
大 scorer / 大 reranker
```

在 30-52 张训练样本下，这些复杂模块普遍不稳定。

当前应保留的小模块是：

```text
1. feature projector
2. PDDL object/location query extractor
3. 1-layer object relation layer
4. spatial coordinate features
5. two_stage placement scorer
6. structured legal-state loss
7. counterfactual margin loss
8. legal PDDL decoder
```

方法主线可以表述为：

```text
frozen DINOv3 H+640 dense features
+ PDDL-conditioned object queries
+ lightweight placement factor scorer
+ structured / counterfactual training
+ legal PDDL decoder
```

## 5. 当前主方法配置

主方法命令配置核心为：

```text
--feature-source dinov3_raw
--dinov3-scales 640
--dinov3-last-n-layers 1
--dinov3-layer-fusion last
--dinov3-add-coords
--d-slot 256
--object-extractor-type object_queries
--object-query-relation-layers 1
--support-head-type two_stage
--support-temperature 1.5
--support-hidden-dim 512
--placement-loss ce_structured
--structured-loss-weight 0.2
--counterfactual-margin-weight 0.2
--counterfactual-margin 1.0
```

不要把后续失败的小模块作为默认主方法。

## 6. 关键实验结果

### 6.1 Few-shot curve, same 100 test split

同一 100 张测试集，训练样本数变化：

| train K | EM | F1 | placement top1/top3/top10 | mean/max rank | missed_stack | location_region |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 30 | 0.6100 | 0.9201 | 0.8138 / 0.8739 / 0.9910 | 1.874 / 12 | 11 | 44 |
| 40 | 0.6300 | 0.9190 | 0.8258 / 0.9099 / 0.9730 | 1.715 / 13 | 13 | 39 |
| 50 | 0.7500 | 0.9484 | 0.8919 / 0.9279 / 0.9940 | 1.399 / 13 | 8 | 19 |
| 52 | 0.7900 | 0.9603 | 0.9129 / 0.9700 / 0.9940 | 1.267 / 13 | 7 | 17 |

重要解释：

```text
0.7900 是 K=52 full train-pool 结果。
0.7500 是 strict K=50 结果。
```

K=50 相比 K=52 少掉的两个训练样本是：

```text
picture_374:
  red_battery -> battery_placement
  其他多个 part -> table

picture_74:
  blue_battery -> table
  blue_pump -> table
  green_regulator -> table
```

100 张 test 上 EM 差 0.04 等价于 4 张图，小样本下这种差异不能忽略。

### 6.2 Backbone / resolution 结果

H+640 相比 H+448 有稳定小幅收益：

| config | EM | F1 | location_region |
| --- | ---: | ---: | ---: |
| H+448 d256 coords structured+CF | 0.7700 | 0.9512 | 21 |
| H+640 d256 coords structured+CF | 0.7900 | 0.9603 | 17 |

结论：

```text
有效提升主要来自更好的 dense visual evidence / spatial resolution，
不是来自更复杂的 trainable scorer。
```

### 6.3 100 train / 52 test 参考结果

之前也跑过 100 train / 52 test：

```text
H+640 d256 coords structured+CF:
EM 0.8462
F1 0.9689
top1/top3/top10 0.9298 / 0.9649 / 1.0000
missed_stack 4
location_region 6
```

但这个 split 与当前 100-test few-shot curve 不同，只作为参考，不和 30/40/50/52 curve 直接混比。

## 7. 已尝试并排除的复杂小模块

这些模块目前不应进入主方法，最多放 appendix / negative ablation。

### 7.1 d_slot=512

H+448 下增大 slot 维度从 256 到 512 反而下降：

```text
H+448 d256: EM 0.7700
H+448 d512: EM 0.6600
```

说明少样本下增加 trainable capacity 会伤害 generalization。

### 7.2 typed_two_stage

把 scorer 拆成：

```text
part_scorer
location_scorer
candidate_bias
stack_gate
```

结果在 52 train / 100 test 下下降：

```text
two_stage baseline:
EM 0.7900
F1 0.9603
location_region 17

typed_two_stage:
EM 0.7200
F1 0.9502
location_region 24
```

结论：问题不是 scorer MLP 不够大，而是 scorer 没有稳定的空间 grounding evidence。拆 head 增参会过拟合。

### 7.3 calibration / dynamic hard negative / reranker

50 train / 100 test 下：

| config | EM | F1 | top1 | location_region |
| --- | ---: | ---: | ---: | ---: |
| baseline two_stage | 0.7500 | 0.9484 | 0.8919 | 19 |
| calibrated | 0.7500 | 0.9459 | 0.8739 | 23 |
| dynamic hard 0.2 | 0.7500 | 0.9507 | 0.8829 | 26 |
| calibrated + dynamic 0.2 | 0.8000 | 0.9585 | 0.9009 | 18 |
| calibrated + dynamic 0.1 | 0.7300 | 0.9484 | 0.8799 | 24 |
| calibrated + dynamic 0.5 | 0.6900 | 0.9364 | 0.8589 | 22 |
| + linear legal reranker | no change | no change | no change | no change |

虽然 `calibrated + dynamic 0.2` 在 strict K=50 上 EM 到 0.80，但它不能直接和 K=52 baseline 的 0.79 比；同时权重敏感，F1/top1/loc 错误没有稳定优势。因此不作为主方法。

### 7.4 attention geometry / location prior / local refinement

这些都基于 query attention 或 early top-k attention：

```text
attention geometry
location prior from attention center
local query refinement
```

结果均为负收益。结论：

```text
当前 query attention 不能当作可靠 object mask / bbox / center 使用。
```

### 7.5 patch-level candidate evidence

尝试了：

```text
base two_stage score
+ location patch evidence
+ optional table residual
+ optional support contact patch-pair evidence
```

50 train / 100 test：

| config | EM | F1 | top1/top3/top10 | missed_stack | location_region |
| --- | ---: | ---: | ---: | ---: | ---: |
| baseline two_stage K50 | 0.7500 | 0.9484 | 0.8919 / 0.9279 / 0.9940 | 8 | 19 |
| patch location scale0 | 0.7200 | 0.9431 | 0.8679 / 0.9520 / 0.9970 | 6 | 27 |
| patch location scale0.1 | 0.7600 | 0.9467 | 0.8739 / 0.9550 / 0.9970 | 10 | 27 |
| patch location+table scale0.1 | 0.7400 | 0.9442 | 0.8709 / 0.9489 / 0.9940 | 5 | 28 |
| patch loc+table+contact scale0.1 | 0.7000 | 0.9424 | 0.8649 / 0.9399 / 0.9970 | 7 | 27 |

结论：

```text
当前 PDDL-query-to-patch similarity 还不是可靠 localization signal。
patch evidence 提高部分 top3/top10，但没有改善 top1，location_region 反而变差。
```

## 8. 当前主要错误与诊断

主要错误集中在：

```text
region/table grounding
stack/contact grounding
support candidate ranking
```

少样本下最明显的问题是 `location_region`：

```text
K=52: location_region 17
K=50: location_region 19
K=40: location_region 39
K=30: location_region 44
```

同时 top10 通常还很高，说明：

```text
gold support/location 经常已经在候选里，
但 top1/top3 排序不稳。
```

这不是 decoder 合法性问题，decoder 已经保证 legal=1.0。瓶颈在：

```text
DINO dense visual features -> PDDL object/location grounding -> support/location ranking
```

## 9. 论文/方法叙事建议

不要把方法写成：

```text
我们训练多个神经模块学习 grounding。
```

应该写成：

```text
We freeze the dense visual backbone and train a lightweight PDDL-conditioned adapter.
The adapter does not learn generic perception; it binds dense visual tokens to named PDDL objects,
factors output into placement assignments, and decodes under symbolic legality constraints.
```

中文表述：

```text
我们冻结视觉 backbone，只训练一个轻量 PDDL 条件适配器。
这个适配器不学习通用视觉，而是把 dense visual tokens 绑定到已知 PDDL 对象和候选 support 上，
并通过 placement factorization 与 legal decoder 降低输出空间复杂度。
```

核心贡献应聚焦：

```text
1. frozen DINOv3 dense visual prior
2. PDDL-conditioned object queries
3. placement factorization
4. structured / counterfactual legal-state training
5. constrained symbolic decoding
```

负结果可作为 appendix 支撑：

```text
few-shot PDDL grounding 中，增加 trainable scorer capacity 或从未监督 attention 中提取 geometry，通常不如保持简单结构和提升 dense visual evidence 稳定。
```

## 10. 当前保留的关键实验目录

清理后保留的主线目录：

```text
experiments/ariac_52_100_hplus640_d256_coords_structcf_20260602
experiments/ariac_40_30_100_hplus640_d256_coords_structcf_baseline_20260602
```

保留的报告文件：

```text
experiments/ariac_100_52_comparison_20260602.md
experiments/ariac_52_100_comparison_20260602.md
experiments/ariac_backbone_scaling_52_100_20260602.md
experiments/ariac_geometry_hybrid_followup_20260602.md
experiments/ariac_hard_samples_52_100_20260602.md
experiments/ariac_module_ablation_typed_head_20260602.md
experiments/ariac_retrain_updated_20260602.md
experiments/ariac_structure_ablation_20260602.md
experiments/ariac_structured_loss_20260602.md
experiments/README.md
```

清理结果：

```text
experiments/ before cleanup: 17GB
experiments/ after cleanup:  about 1.3GB
```

删除的是 smoke、失败小模块、patch evidence、strict K=50、旧 ablation/optimization/cache、旧 AEPaQ/fair/primitive/support-decoder 等中间实验目录。没有删除 `data/`、DINOv3 权重、K=52 主线 H+640 特征缓存或主线结果。

## 11. 当前代码状态

当前代码里保留了若干 optional flags，但默认主方法不启用它们：

```text
calibrated_two_stage
dynamic hard-negative loss
linear legal reranker
attention geometry
location prior
local refinement
patch-level candidate evidence
```

这些用于诊断/appendix，不应作为当前主方法默认配置。

验证通过：

```text
python3 -m py_compile paq/support_head.py paq/model.py paq/slot_attention.py training/run_ariac_structured.py training/predict_ariac_init.py
python3 -m pytest tests/test_blocksworld_support.py
```

## 12. 下一步建议

优先方向：

```text
1. 保持当前主方法简单，不继续堆复杂 scorer。
2. 做多 split / 多 seed，报告 mean ± std。
3. 若要提升，优先尝试更强 frozen evidence：
   - H+768
   - 7B 448
   - 7B 640
4. 如果允许额外轻监督，优先标注少量 placement region bbox/polygon 或 part center。
5. 如果继续改 loss，只做低参数 calibration / hard-negative weighting，并必须跨 split 验证。
```

不建议继续优先做：

```text
d_slot=512
typed_two_stage
attention geometry as bbox
location prior from attention center
current local refinement
current patch evidence
large reranker / large relation MLP
```

一句话总结：

```text
当前最可靠的方法不是训练复杂小模块，而是使用 frozen DINOv3 dense features，
通过轻量 PDDL-conditioned adapter 进行 object binding、placement factorization、
structured ranking 和 legal decoding。
```

## 13. 2026-06-03 label / region token / PSLG 实验更新

新增了 `/home/pc/pddl_image/data/ariac/labels` 中的 YOLO bbox 标注。该标注只有物体类别，没有颜色；严格可用 label 样本少于完整 split，因此不能直接作为主线监督。

label bbox / region token 结果：

```text
full split baseline:
  EM 0.7900, F1 0.9603, top1/top3/top10 0.9129/0.9700/0.9940

full + bbox geometry:
  EM 0.7200

full region-only token:
  EM 0.5400

full region+dense hybrid:
  EM 0.7200

label-only subset dense baseline:
  EM 0.6207

label-only subset region-only h256:
  EM 0.6552
```

结论：region token 对 label-only subset 有一点信号，能改善部分 location ranking；但 full split 上明显掉分，主要原因是不完整 bbox label 带来 proposal coverage bias。它适合作为诊断或 appendix，不适合作为当前主方法。

PSLG / latent grounding 实验结果：

```text
baseline:
  EM 0.7900, F1 0.9603, top1/top3/top10 0.9129/0.9700/0.9940

listwise-heavy:
  EM 0.7400

heatmap-only object extractor:
  EM 0.6100

heatmap + listwise + latent grounding:
  EM 0.6300

iterative object queries + latent grounding 0.1:
  EM 0.7900, location_region 17 -> 13, missed_stack 7 -> 10

iterative object queries + loc-only latent 0.05:
  EM 0.7800

iterative object queries + loc-only latent 0.10:
  EM 0.7000
```

legal-state oracle 诊断：

```text
state oracle top1  EM = 0.7900
state oracle top3  EM = 0.8300
state oracle top5  EM = 0.8600
state oracle top10 EM = 0.9200
state oracle top25 EM = 0.9400
```

结论：

```text
1. aggressive listwise training 会破坏当前 calibrated placement scorer。
2. naive heatmap query pooling 太弱，不能替代 iterative object query extractor。
3. 当前 query attention 仍不能当可靠 object/location grounding heatmap。
4. latent grounding 的 location 改善信号不稳定；loc-only 版本没有保住收益。
5. PSLG 思路有研究价值，但当前实现不能超过 H+640 baseline。
```

当前主方法仍应保持：

```text
DINOv3 H+640 frozen dense features
+ d_slot=256
+ coords
+ PDDL object queries
+ two_stage placement scorer
+ structured+CF
+ legal decoder
```

新增实验报告：

```text
experiments/ariac_label_bbox_ablation_20260603.md
experiments/ariac_region_token_ablation_20260603.md
experiments/ariac_pslg_listwise_heatmap_20260603.md
```

## 14. 2026-06-03 PSLG 100 train / 52 test 复测

为了确认 PSLG/listwise/heatmap 变体是否只是 52 train 下数据太少，又用 100 train / 52 test 复测了一轮。该轮使用 H+640 frozen dense features、d_slot=256、coords、two_stage、structured+CF，训练 120 epoch。

结果：

```text
H+640 baseline:
  EM 0.8462, F1 0.9723, top1/top3/top10 0.9415/0.9766/1.0000

listwise-heavy:
  EM 0.8269, F1 0.9654

iterative + latent grounding 0.1:
  EM 0.8654, F1 0.9711

heatmap-only:
  EM 0.5962, F1 0.9264

iterative + loc-only latent 0.05:
  EM 0.8269, F1 0.9574

iterative + loc-only latent 0.10:
  EM 0.8269, F1 0.9689

iterative + small topK 0.2:
  EM 0.8269, F1 0.9666
```

解释：

```text
1. 更多训练样本没有救回 heatmap-only，也没有救回 heavy/small listwise。
2. loc-only latent grounding 仍然不如 baseline。
3. full latent grounding 0.1 在 EM 上多对 1 张测试图：
   0.8462 -> 0.8654。
4. 但这个提升不干净：
   F1 0.9723 -> 0.9711，
   top1 0.9415 -> 0.9357，
   top3 0.9766 -> 0.9649，
   location_region 4 -> 6。
```

结论：

```text
full latent grounding 0.1 可以保留为 tentative ablation，
但还不能替代主线 baseline。要证明它有用，必须做 multi-seed / multi-split；
单个 52-test split 上多对 1 张不足以说明方法稳定提升。
```

新增报告：

```text
experiments/ariac_pslg_k100_52_20260603.md
```

## 15. 2026-06-03 workspace calibration / labels.csv 实验

新增 `/home/pc/pddl_image/data/ariac/labels.csv`，其中包含一次性静态 workspace location 标定：

```text
table
pump_placement
regulator_placement
buffer_placement
battery_placement
```

实现了两个可选用法：

```text
1. workspace top-K legal-state rerank：
   用 object-query attention center 估计动态 part center，
   用 labels.csv 的静态 location bbox 给 top-K legal assignments 加几何分。

2. workspace location query init：
   用 labels.csv bbox center 替换 object_slot_init 中 hardcoded location 坐标。
```

52 train / 100 test 当前代码重跑：

```text
current baseline:
  EM 0.7200, F1 0.9442

workspace rerank w=1:
  EM 0.7200, F1 0.9436

workspace rerank w=5:
  EM 0.7200, F1 0.9454

workspace init location queries:
  EM 0.6800, F1 0.9431
```

100 train / 52 test：

```text
H+640 baseline:
  EM 0.8462, F1 0.9723

workspace rerank w=5:
  EM 0.8462, F1 0.9723

workspace init location queries:
  EM 0.8269, F1 0.9654
```

attention-center 诊断：

```text
gold named-region part_at cases: 33
attention center inside correct named region: 15 / 33 = 45.45%

gold table part_at cases: 123
attention center inside table residual: 121 / 123 = 98.37%
```

结论：

```text
labels.csv 作为 workspace calibration 是合理的环境先验，
但当前 object-query attention center 不是可靠动态 part center。
因此 workspace polygon 直接 rerank 没有提升；
用 labels.csv center 替换 location query init 也会掉分。

这说明下一步如果要利用 workspace calibration，
必须先有更可靠的动态 part localizer / proposal / object-region matching。
```

新增报告：

```text
experiments/ariac_workspace_oracle_20260603.md
```

### workspace 纯评估修正

之前 workspace 实验有一个解释问题：部分结果来自重新训练，导致 52/100 baseline 从历史 checkpoint 的 0.7900 漂移到 0.7200。为避免混淆，新增纯评估诊断：

```text
training/diagnose_workspace_rerank.py
```

该脚本固定同一个 checkpoint 和 support_scores，只比较三种 decode：

```text
A. normal legal decoder
B. workspace rerank + object-query attention center
C. workspace rerank + available part bbox center，缺失时 fallback attention center
```

52 train / 100 test 历史 checkpoint：

```text
A normal:
  EM 0.7900, F1 0.9603

B workspace + attention center:
  EM 0.7800, F1 0.9615

C workspace + bbox center:
  EM 0.7900, F1 0.9638

gold legal state in top10:
  92/100 = 0.9200

assigned active part bbox centers:
  149/333 = 0.4474

A -> C changed:
  bad_to_good: picture_307
  good_to_bad: picture_323
```

100 train / 52 test checkpoint：

```text
A normal:
  EM 0.8462, F1 0.9723

B workspace + attention center:
  EM 0.8462, F1 0.9723

C workspace + bbox center:
  EM 0.8462, F1 0.9723

gold legal state in top10:
  49/52 = 0.9423

assigned active part bbox centers:
  95/171 = 0.5556

A -> B/C changed:
  0 images
```

修正后的结论：

```text
labels.csv 不能被否掉。它作为 static workspace calibration 是合理的。
但当前 workspace top-K rerank 还不能稳定提升 EM：
  - attention center 版本会伤一张；
  - bbox center 版本 52/100 修一张又伤一张；
  - 100/52 完全不改变 top1。

要让 workspace calibration 真正发挥作用，需要更强的动态 part center/proposal 覆盖，
或者把 workspace 几何作为训练/候选生成的一部分，而不只是简单 top-K 加分。
```

新增详细报告：

```text
experiments/ariac_workspace_pure_eval_diagnostic_52_100_20260603.md
experiments/ariac_workspace_pure_eval_diagnostic_100_52_20260603.md
```

## 16. 2026-06-03 失败方案目录清理

已清理 `experiments/` 中当前尝试但没有成为主线的训练目录和中间模型，只保留报告结论。

删除的目录类型：

```text
label bbox / label-only 训练目录
region token / region+dense hybrid 训练目录
PSLG / heatmap / listwise / loc-only 训练目录
K100 PSLG 训练目录
workspace rerank / init-location 重训练目录
```

保留：

```text
experiments/ariac_52_100_hplus640_d256_coords_structcf_20260602/
experiments/ariac_k100_hplus640_baseline_20260603/
experiments/ariac_40_30_100_hplus640_d256_coords_structcf_baseline_20260602/
```

以及所有用于说明结论的 markdown 报告。

当前 `experiments/` 约 1.3GB，只剩一份真实 H+640 feature cache：

```text
experiments/ariac_52_100_hplus640_d256_coords_structcf_20260602/ariac_dinov3_raw_s640_l1_last_coords_features.pt
```

其他保留目录中的 feature cache 都是软链接。

## 17. 2026-06-03 PDDL-Cache Verifier 测试

新增纯评估脚本：

```text
training/diagnose_cache_verifier.py
```

实现内容：

```text
H+640 checkpoint 作为 candidate generator
object slots + support-score/margin/type bits 构造 edge memory key
table / region / support_part 至少三类 bucket
可选 state-pattern memory
train leave-one-out 选 k/beta/lambda，test 不调参
topK legal states 内 rerank
```

主结果：

```text
52/100:
  normal EM/F1 = 0.7900 / 0.9603
  cache  EM/F1 = 0.7800 / 0.9591
  changed 1, bad_to_good 0, good_to_bad 1

100/52:
  normal EM/F1 = 0.8462 / 0.9723
  cache  EM/F1 = 0.8462 / 0.9723
  changed 0

40/100:
  normal EM/F1 = 0.6300 / 0.9190
  cache  EM/F1 = 0.6300 / 0.9213
  changed 2, both bad_to_bad with gold not in top10

30/100:
  normal EM/F1 = 0.6100 / 0.9201
  cache  EM/F1 = 0.6100 / 0.9201
  changed 0
```

52/100 额外诊断：

```text
part-kind bucket: 0.7800 / 0.9591
Tip-style logsumexp: unchanged at 0.7900 / 0.9603
forced stronger edge lambda=0.5: 0.7800 / 0.9591
forced state memory lambda=0.5: unchanged at 0.7900 / 0.9603
```

结论：

```text
当前 PDDL-cache verifier 是 no-go。
假设本身合理，但现有 object-slot/cache feature space 没有形成可复用的 placement-case 检索结构。
top10 oracle 的 0.92 空间仍在，但不能靠当前内部 slots + support score/type features 的 non-parametric memory 稳定拿到。
```

总报告：

```text
experiments/ariac_pddl_cache_verifier_20260603.md
```

## 18. 2026-06-03 PVG Proposal-Verified Geometry Reranker

新增纯评估脚本：

```text
training/diagnose_pvg_rerank.py
```

实现：

```text
H+640 baseline candidate generator
top10/top25 legal states
proposal-derived part boxes/centers
workspace/root geometry
stack bbox geometry
atom-branch likelihood
conservative gated rerank
```

proposal sources：

```text
hsv: deterministic HSV connected components，当前默认主 proposal source
label: data/ariac/labels YOLO class-only boxes + color assignment，仅作为 ablation，不作为主方法
```

52/100 label-only forced top25 high-geometry：

```text
proposal coverage:
  active centers 149/333 = 0.4474
  root centers   141/306 = 0.4608
  stack pairs      8/27  = 0.2963

normal:
  EM 0.7900, F1 0.9603

C_geometry:
  EM 0.8000, F1 0.9627
  changed 1
  bad_to_good 1: picture_307
  good_to_bad 0
```

52/100 label+HSV narrow LOO/grid：

```text
proposal coverage:
  active centers 332/333 = 0.9970
  root centers   305/306 = 0.9967
  stack pairs     27/27  = 1.0000
  source counts: label=149, hsv=183

B_atom / C_geometry / D_pvg:
  all unchanged at EM 0.7900, F1 0.9603
```

原因：

```text
train split baseline 已经 EM=1.0。
按 EM/F1/fewer-changed 的 LOO 规则，所有不伤 train 的配置打平时会选择 no-op。
```

52/100 label+HSV forced top25 high-geometry：

```text
tau=0:
  EM 0.7900, F1 0.9615
  bad_to_good 1: picture_307
  good_to_bad 1: picture_336

tau=0.5:
  EM 0.8000, F1 0.9627
  bad_to_good 1: picture_307
  good_to_bad 0
```

诊断：

```text
picture_307 修复 margin 约 10.24，使用 YOLO label boxes。
picture_336 伤图 margin 约 0.037，proposal 证据全来自 HSV component。
tau=0.5 可以拒掉这类低 margin HSV 误改。
```

完全移除 YOLO label boxes 后的 HSV-only 结果：

```text
proposal coverage:
  active centers 326/333 = 0.9790
  root centers   299/306 = 0.9771
  stack pairs     26/27  = 0.9630

narrow LOO/grid:
  B_atom / C_geometry / D_pvg 全部 unchanged
  EM 0.7900, F1 0.9603

forced top25 high-geometry tau=0.5:
  EM 0.8000, F1 0.9620
  changed 2
  bad_to_good 1: picture_307
  good_to_bad 0
  bad_to_bad 1: picture_63
```

结论：

```text
PVG 比 object-slot cache 更有真实信号：能安全修 picture_307。
但当前低成本 HSV-only proposal bank 只有 +1 net fix，远低于冲 0.9 需要的约 +11 net fixes。
HSV 解决 coverage，不解决可靠 part identity / object-region matching。
下一步不要继续调 lambda，应该接更强 proposal identity：GroundingDINO / OWL-ViT / SAM2 或 DINO crop kind prototype。
```

总报告：

```text
experiments/ariac_pvg_rerank_20260603.md
```
